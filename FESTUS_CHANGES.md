
# Festus Custom Changes

Date: autogenerated

## Settings
- Default **owner** set to `254714091722` in `settings.js` (can be overridden with `OWNER_NUMBER` env).
- Default **author/owner name** set to `Festus` via `AUTHOR` in `settings.js`.

## Environment (.env.example)
Added keys:
```
OWNER_NUMBER=254714091722
AUTHOR=Festus
AUTOREPLY_DM=true
AUTOREPLY_DM_COOLDOWN_MINUTES=10
AUTOREPLY_GROUP_MENTIONS=true
```

## Autoreplies (main.js)
- **DM autoresponder**: Replies in private chats with:
  > Festus will be back in a short while. How can I assist you?
  Uses a per-chat cooldown (default 10 minutes). Toggle via `AUTOREPLY_DM`.
- **Group mention autoresponder**: When you are tagged/mentioned in groups, replies with:
  > Hey, how can I help? Festus is unavailable.
  Uses a per-group cooldown (default 10 minutes). Toggle via `AUTOREPLY_GROUP_MENTIONS`.

You can adjust messages by editing constants near the top of `main.js` marked "FESTUS CUSTOM AUTOREPLIES".

## Notes
- Social media handles were removed from runtime UI where present; branding links in comments and README are unchanged.
- If you want to disable auto-DM or group mentions, set the env flags to `false`.


## Keyword autoresponder
- Triggers on words: `answers`, `bms`, `onboarding`, `anyone` (case-insensitive; whole-word aware).
- Responds in both private chats and groups with:
  > Hey! Message us right now.
- Toggle with `AUTOREPLY_KEYWORDS` and customize via `KEYWORD_TRIGGERS` (comma-separated).
